spring:
  application:
    name: business-common
  profiles:
    active: ut
server:
  servlet:
    context-path: /business-common
  port: 8210

my:
  spring:
    redis:
      port: 7379
      pool:
        minIdle: 0
        maxIdle: 10
        maxActive: 10
